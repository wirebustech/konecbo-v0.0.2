import React from 'react';
import { Box, Typography } from '@mui/material';

const ReviewForm = () => {
  return (
    <Box sx={{ p: 3, textAlign: 'center', bgcolor: '#fff' }}>
      <Typography>Review Form Unavailable</Typography>
    </Box>
  );
};
export default ReviewForm;
