import React from 'react';
import { Box, Typography } from '@mui/material';

const FriendProfile = () => {
  return (
    <Box sx={{ p: 3, textAlign: 'center' }}>
      <Typography variant="h6">Friend Profile Unavailable</Typography>
      <Typography variant="body2">This feature is currently under maintenance.</Typography>
    </Box>
  );
};

export default FriendProfile;